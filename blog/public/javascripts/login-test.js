 debugger
 //var submitButton = $(".bindSpan");
 //submitButton.on("click", submit);

 function submit() {
     var userName = $(this).text();
     var passward = $(this).text();
     alert("name" + userName);
 }